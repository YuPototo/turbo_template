{
  "name": "@company-name/database",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "dependencies": {
    "@prisma/client": "latest"
  },
  "devDependencies": {
    "prisma": "4.15.0"
  },
  "scripts": {
    "//db:generate": "echo 'generate assets like Prisma Client based on the datasource and generator blocks defined in your schema.prisma file'",
    "db:generate": "prisma generate",
    "//db:push": "echo 'synchronize your Prisma schema with your database schema. The --skip-generate flag on db:push to ensure it doesn't automatically run prisma generate after migrating the database. This ends up being faster when using Turborepo because we automatically parallelize the tasks.'",
    "db:push": "prisma db push --skip-generate"
  }
}
